/**
 * Stable package root. New consumers should prefer the explicit versioned
 * subpaths under `pi-maestro-teammate/v1/*` when they only need one API area.
 */
export * from "./public/v1/index.ts";
